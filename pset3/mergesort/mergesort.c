#include <cs50.h>
#include <stdio.h>

/*
Got blocked since it is way more complicated than I 
expected to dynamically get the length of the array.  
*/ 

//Left, right and original destination array to merge
void merge(int l[], int r[], int a[])
{
    return;
}

void mergesort(int arr[])
{
    return;
}

int main(int argc, int argv)
{

}